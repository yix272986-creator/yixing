# 《AI发展史：1950—2026》设计说明

## 视觉定位
深色未来科技纪录片风格，用克制的青蓝和紫色构建数据档案、HUD、扫描线、节点和轨迹线，面向抖音竖屏发布。

## 色彩系统
- 背景：#050816
- 深层背景：#090D22
- 青蓝：#45E6FF
- 亮蓝：#4A7DFF
- 紫色：#A56BFF
- 正文：#F2F5FF
- 次要文字：#AAB4D6
- 寒冬警示：#FF617E

## 字体策略
使用本地可回退中文字体栈，并通过 `@font-face local(...)` 声明。标题使用粗重黑体风格，年份与技术标签使用等宽风格，正文使用清晰中文系统字体。不使用网络字体。

## 安全区
重要文字左右边距至少 90px；底部字幕距离底边至少 150px；标题、正文和标签均限制在 1080×1920 画布内。

## 九个场景时间范围
1. 0—6秒：开场，1950—2026。
2. 6—13秒：图灵，1950。
3. 13—20秒：达特茅斯会议，1956。
4. 20—28秒：AI寒冬，1970s—1990s。
5. 28—35秒：Deep Blue，1997。
6. 35—43秒：AlexNet，2012。
7. 43—51秒：AlphaGo，2016。
8. 51—61秒：生成式AI，2022。
9. 61—72秒：Agent未来，2026 →。

## 信息层级
场景内依次强调年份、标题、图形、正文、技术标签或最终字幕，避免同一时间出现过多可读信息。

## 入场动画规则
所有场景使用 GSAP `from()` 或 `fromTo()` 入场；标题入场约 0.5—0.9 秒，正文约 0.4—0.7 秒。动画可 seek、确定性播放，不使用 `setTimeout` 或随机数。

## 转场规则
主要转场为数据光束横向擦除；AI 寒冬前后使用轻微模糊交叉感的透明层；AlphaGo 到生成式AI 使用短促数字故障层。全片不使用廉价马赛克或频繁闪白。

## 最终渐隐规则
只有 69—72 秒允许最终场景整体平缓 color dip to black，其他场景不做内容单独退出动画。

## 禁止使用的视觉风格
避免廉价霓虹灯、过度闪烁、真实人物照片、受版权保护画面、外部图片、网络字体、在线脚本和 WebGL shader 依赖。

## 后续扩展旁白和音乐
当前项目不包含旁白音频和音乐。后续可增加本地生成或授权音频文件，并在 composition 内添加 `<audio class="clip">`，同时保持本地引用和验证脚本检查。
